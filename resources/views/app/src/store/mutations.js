export const SET_AUTH_USER = (state, user) => {
  state.authUser = user;
}

export const SET_DATA = (state, data) => {
  state.currentViewData = data;
}

export const SET_HEAD = (state, data) => {
  state.head = data;
}

